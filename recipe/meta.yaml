{% set name = "carto-auth" %}
{% set version = "0.1.0b5" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 52ea592a83d0ee32dd2222ce1a859458240f013df81fdc0b70c83709033f3672

build:
  number: 0
  script: {{ PYTHON }} -m pip install . -vv
  noarch: python

requirements:
  host:
    - python >=3.7
    - pip
  run:
    - python >=3.7
    - requests

test:
  imports:
    - carto_auth

about:
  home: https://github.com/cartodb/carto-auth
  summary: Python library to authenticate with CARTO
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  doc_url: https://github.com/CartoDB/carto-auth/tree/master/docs
  dev_url: https://github.com/cartodb/carto-auth

extra:
  recipe-maintainers:
    - Jesus89
